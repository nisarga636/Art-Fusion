import type { Meta, StoryObj } from "@storybook/react";
import AlertBox from "../components/AlertBox";


const meta: Meta<typeof AlertBox> = {
    title: "ui/AlertBox",
    component: AlertBox,
  };

  export default meta;

  type Story = StoryObj<typeof AlertBox>;


  export const AlertBoxcenter: Story = {
  
  };